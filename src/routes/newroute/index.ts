import { ResponseService } from '../../services';

export default {
    serviceName: new ResponseService(),
    middlewares: [],
    URL: '/abc',
    httpMethod: 'post',
    // authenticated: true,
};
